<%= form_with(model: mokatba, local: true) do |form| %>
  <% if mokatba.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mokatba.errors.count, "error") %> prohibited this mokatba from being saved:</h2>

      <ul>
        <% mokatba.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :attachment %>
    <%= form.file_field :attachedFile %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
